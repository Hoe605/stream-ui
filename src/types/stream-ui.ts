export type {
    ComponentMap,
    RenderMode,
    StackNode,
    StreamBlockCategory,
    StreamBlockData,
    StreamBlockReporter,
    StreamContainsProps
} from '../core/stream-contains-core';

export { streamContainsProps } from '../core/stream-contains-core';
